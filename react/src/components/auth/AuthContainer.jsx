import styles from './AuthContainer.module.css'

const AuthContainer = ({}) => {
    return (
        <> 
        <div className={styles.authContainer}>Авторизация</div>
        </>
       
    )
}

export default AuthContainer;